import 'package:flutter/material.dart';

class EventMapTab extends StatelessWidget {
  const EventMapTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Event Map'), centerTitle: true),
      body: const Center(child: Text('Event Map Tab - Coming Soon')),
    );
  }
}
